{% extends 'layout/base.html.twig' %}

{% block body %}
    <main class="main">
        <section class="checker-section">
            <div class="checker-card">
                <h2>Проверить DKIM запись</h2>
                <p class="checker-description">
                    DKIM (DomainKeys Identified Mail) - это метод аутентификации электронной почты, который использует криптографические подписи для подтверждения подлинности отправителя и целостности сообщения.
                </p>

                <div class="checker-form">
                    <div class="form-group">
                        <label for="domainInput">Введите доменное имя:</label>
                        <input
                            type="text"
                            id="domainInput"
                            class="checker-input"
                            placeholder="example.com"
                            autocomplete="off"
                        >
                    </div>
                    <div class="form-group">
                        <label for="selectorInput">Введите DKIM селектор (необязательно):</label>
                        <input
                            type="text"
                            id="selectorInput"
                            class="checker-input"
                            placeholder="default"
                            autocomplete="off"
                        >
                        <small class="form-hint">Обычные селекторы: default, google, k1, s1, dkim</small>
                    </div>
                    <button class="checker-btn" id="checkBtn">
                        <span class="btn-icon">🔍</span>
                        <span class="btn-text">Проверить DKIM</span>
                    </button>
                </div>
            </div>
        </section>

        <section class="results-section" id="resultsSection" style="display: none;">
            <div class="results-card">
                <div class="results-header">
                    <h2>Результаты проверки</h2>
                    <div class="status-badge" id="statusBadge">Проверка...</div>
                </div>

                <div class="results-content">
                    <div class="result-item">
                        <div class="result-label">Домен:</div>
                        <div class="result-value" id="resultDomain">-</div>
                    </div>

                    <div class="result-item">
                        <div class="result-label">Селектор:</div>
                        <div class="result-value" id="resultSelector">-</div>
                    </div>

                    <div class="result-item">
                        <div class="result-label">DKIM запись:</div>
                        <div class="result-value code-block" id="resultRecord">-</div>
                    </div>

                    <div class="result-item">
                        <div class="result-label">Статус:</div>
                        <div class="result-value" id="resultStatus">-</div>
                    </div>

                    <div class="result-details" id="resultDetails"></div>

                    <div class="info-box">
                        <h3>Анализ записи</h3>
                        <div id="analysisContent">Ожидание результатов...</div>
                    </div>

                    <div class="info-box" id="warningsBox" style="display: none;">
                        <h3>Предупреждения</h3>
                        <ul id="warningsList"></ul>
                    </div>
                </div>
            </div>
        </section>

        <section class="info-section">
            <div class="info-card">
                <h2>О DKIM записях</h2>
                <div class="info-content">
                    <h3>Что такое DKIM?</h3>
                    <p>
                        DKIM (DomainKeys Identified Mail) - это метод аутентификации электронной почты, который позволяет получателю проверить, что письмо действительно было отправлено авторизованным сервером домена и не было изменено при передаче.
                    </p>

                    <h3>Как работает DKIM?</h3>
                    <ul>
                        <li>Отправляющий сервер добавляет цифровую подпись к заголовкам письма</li>
                        <li>Подпись создается с использованием приватного ключа</li>
                        <li>Публичный ключ публикуется в DNS записи</li>
                        <li>Получающий сервер проверяет подпись с помощью публичного ключа</li>
                    </ul>

                    <h3>Пример DKIM записи:</h3>
                    <div class="code-example">
                        v=DKIM1; k=rsa; p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQC...
                    </div>

                    <h3>Основные параметры:</h3>
                    <ul>
                        <li><strong>v=</strong> - версия протокола (DKIM1)</li>
                        <li><strong>k=</strong> - тип ключа (rsa, ed25519)</li>
                        <li><strong>p=</strong> - публичный ключ в формате Base64</li>
                        <li><strong>t=</strong> - флаги (s - строгий режим, y - тестовый режим)</li>
                        <li><strong>h=</strong> - допустимые алгоритмы хеширования</li>
                    </ul>

                    <h3>Преимущества DKIM:</h3>
                    <ul>
                        <li>Подтверждение подлинности отправителя</li>
                        <li>Защита от изменения содержимого письма</li>
                        <li>Повышение доставляемости</li>
                        <li>Улучшение репутации домена</li>
                    </ul>

                    <h3>Распространенные селекторы:</h3>
                    <ul>
                        <li><strong>default</strong> - стандартный селектор</li>
                        <li><strong>google</strong> - используется Google</li>
                        <li><strong>k1, s1</strong> - часто используются почтовыми провайдерами</li>
                        <li><strong>dkim</strong> - общий селектор</li>
                    </ul>
                </div>
            </div>
        </section>
    </main>
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/checker.css') }}">
{% endblock %}
